server:
  port: 8989
  servlet:
    context-path: /bank-api

spring:
  security:
    user:
      name: bankapp
      password: changeit
  h2:
    console:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
